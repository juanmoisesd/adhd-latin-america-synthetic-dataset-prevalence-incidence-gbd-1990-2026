name: Sign Release

on:
  release:
    types: [published]

jobs:
  sign:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Cosign
        uses: sigstore/cosign-installer@main
      - name: Sign Dataset
        run: |
          echo "Signing with Cosign..."
          # cosign sign-blob --key cosign.key data/raw/adhd_data.csv
