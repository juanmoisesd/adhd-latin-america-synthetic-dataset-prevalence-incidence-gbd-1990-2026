# Randomness Policy

To ensure reproducibility, all stochastic processes in this project must use a fixed random seed.
The default seed is `42`.
All scripts must allow setting the seed via command-line arguments or environment variables.
